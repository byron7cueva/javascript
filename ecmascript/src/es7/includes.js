// * Includes
// Trabaja sonbre un arreglo o un string
// Nos permite saber si hay un elemento dentro de estos valores

let numbers = [1, 2, 3, 7, 8];

if (numbers.includes(7)) {
  console.log('Si se encuentra el valor 7');
} else {
  console.log('No se encuentra');
}