// * Object.values
// Devuelve un arragle con los valores
// que tengo en un objeto

const data = {
  frontend: 'Byron',
  backend: 'David',
  design: 'Gaby',
  age: 27
}

const values = Object.values(data);
console.log(values);